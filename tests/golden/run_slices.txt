$ smpc3 run examples/slices.smpc
--- код возврата: 0

[SKILL ISSUE :: W0301] in examples/slices.smpc:15:39
  15 | [#arena:0]  *&M<f32:3,4> -> @alloc => $m;
     |                                       ^~
     |-- ОШИБКА: Записанное значение ни разу не прочитано.
     |-- ДЕТАЛИ: В регистр $m записано значение, но ни одна инструкция его не читает.
     |-- ДИАГНОЗ: Ты складываешь сущности, у которых нет общей природы.
     +-- ИСПРАВЛЕНИЕ: Либо используй результат, либо не вычисляй его.

[SKILL ISSUE :: W0301] in examples/slices.smpc:23:30
  23 | *&M[1, ..] -> @reduce.add => $row1;
     |                              ^~~~~
     |-- ОШИБКА: Записанное значение ни разу не прочитано.
     |-- ДЕТАЛИ: В регистр $row1 записано значение, но ни одна инструкция его не читает.
     |-- ДИАГНОЗ: Система типов существует именно для таких, как ты.
     +-- ИСПРАВЛЕНИЕ: Либо используй результат, либо не вычисляй его.

[SKILL ISSUE :: W0301] in examples/slices.smpc:26:38
  26 | [^raw]  *&M[0, ..] -> @reduce.add => $row0_raw;
     |                                      ^~~~~~~~~
     |-- ОШИБКА: Записанное значение ни разу не прочитано.
     |-- ДЕТАЛИ: В регистр $row0_raw записано значение, но ни одна инструкция его не читает.
     |-- ДИАГНОЗ: Ты складываешь сущности, у которых нет общей природы.
     +-- ИСПРАВЛЕНИЕ: Либо используй результат, либо не вычисляй его.

[SKILL ISSUE :: W0301] in examples/slices.smpc:30:30
  30 | *&M[.., 2] -> @reduce.add => $col2;
     |                              ^~~~~
     |-- ОШИБКА: Записанное значение ни разу не прочитано.
     |-- ДЕТАЛИ: В регистр $col2 записано значение, но ни одна инструкция его не читает.
     |-- ДИАГНОЗ: Система типов существует именно для таких, как ты.
     +-- ИСПРАВЛЕНИЕ: Либо используй результат, либо не вычисляй его.

[SKILL ISSUE :: W0301] in examples/slices.smpc:38:47
  38 | [^raw]  *&M[.., 2] -> @pack -> @reduce.add => $col2_raw;
     |                                               ^~~~~~~~~
     |-- ОШИБКА: Записанное значение ни разу не прочитано.
     |-- ДЕТАЛИ: В регистр $col2_raw записано значение, но ни одна инструкция его не читает.
     |-- ДИАГНОЗ: Система типов существует именно для таких, как ты.
     +-- ИСПРАВЛЕНИЕ: Либо используй результат, либо не вычисляй его.

[SKILL ISSUE :: W0301] in examples/slices.smpc:42:29
  42 | *&M[2, 3] -> @reduce.add => $last;
     |                             ^~~~~
     |-- ОШИБКА: Записанное значение ни разу не прочитано.
     |-- ДЕТАЛИ: В регистр $last записано значение, но ни одна инструкция его не читает.
     |-- ДИАГНОЗ: Ты складываешь сущности, у которых нет общей природы.
     +-- ИСПРАВЛЕНИЕ: Либо используй результат, либо не вычисляй его.

[SKILL ISSUE :: W0301] in examples/slices.smpc:50:24
  50 | *&M  -> @reduce.add => $sum_m;
     |                        ^~~~~~
     |-- ОШИБКА: Записанное значение ни разу не прочитано.
     |-- ДЕТАЛИ: В регистр $sum_m записано значение, но ни одна инструкция его не читает.
     |-- ДИАГНОЗ: Ты складываешь сущности, у которых нет общей природы.
     +-- ИСПРАВЛЕНИЕ: Либо используй результат, либо не вычисляй его.

[SKILL ISSUE :: W0301] in examples/slices.smpc:51:24
  51 | *&MT -> @reduce.add => $sum_mt;
     |                        ^~~~~~~
     |-- ОШИБКА: Записанное значение ни разу не прочитано.
     |-- ДЕТАЛИ: В регистр $sum_mt записано значение, но ни одна инструкция его не читает.
     |-- ДИАГНОЗ: Неявных приведений нет. Придётся думать.
     +-- ИСПРАВЛЕНИЕ: Либо используй результат, либо не вычисляй его.

[SKILL ISSUE :: W0301] in examples/slices.smpc:55:39
  55 | [#arena:0]  *&R<f32:3,4> -> @alloc => $r;
     |                                       ^~
     |-- ОШИБКА: Записанное значение ни разу не прочитано.
     |-- ДЕТАЛИ: В регистр $r записано значение, но ни одна инструкция его не читает.
     |-- ДИАГНОЗ: Ты складываешь сущности, у которых нет общей природы.
     +-- ИСПРАВЛЕНИЕ: Либо используй результат, либо не вычисляй его.

[SKILL ISSUE :: W0301] in examples/slices.smpc:58:23
  58 | *&R -> @reduce.add => $sum_r;    // (1+2+3+4 + 9+10+11+12) * 100 = 5200
     |                       ^~~~~~
     |-- ОШИБКА: Записанное значение ни разу не прочитано.
     |-- ДЕТАЛИ: В регистр $sum_r записано значение, но ни одна инструкция его не читает.
     |-- ДИАГНОЗ: Неявных приведений нет. Придётся думать.
     +-- ИСПРАВЛЕНИЕ: Либо используй результат, либо не вычисляй его.
$row1         <f32>  26
$row0_raw     <f32>  10
$col2         <f32>  21
$col2_raw     <f32>  21
$last         <f32>  12
$sum_m        <f32>  78
$sum_mt       <f32>  78
$sum_r        <f32>  5200
*&M           <f32:3,4>  [1, 2, 3, 4, 5, 6, 7, 8, ... (+4)]
*&MT          <f32:4,3>  [1, 5, 9, 2, 6, 10, 3, 7, ... (+4)]
*&R           <f32:3,4>  [100, 200, 300, 400, 0, 0, 0, 0, ... (+4)]
