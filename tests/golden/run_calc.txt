$ smpc3 run examples/calc.smpc
--- код возврата: 0

[SKILL ISSUE :: W0301] in examples/calc.smpc:16:19
  16 | 17 -> @mul(23) => $product;              // 391, i32
     |                   ^~~~~~~~
     |-- ОШИБКА: Записанное значение ни разу не прочитано.
     |-- ДЕТАЛИ: В регистр $product записано значение, но ни одна инструкция его не читает.
     |-- ДИАГНОЗ: Приведение пишется явно. Молча округлять язык не станет.
     +-- ИСПРАВЛЕНИЕ: Либо используй результат, либо не вычисляй его.

[SKILL ISSUE :: W0301] in examples/calc.smpc:17:20
  17 | 100 -> @sub(37) => $difference;          // 63
     |                    ^~~~~~~~~~~
     |-- ОШИБКА: Записанное значение ни разу не прочитано.
     |-- ДЕТАЛИ: В регистр $difference записано значение, но ни одна инструкция его не читает.
     |-- ДИАГНОЗ: Ты складываешь сущности, у которых нет общей природы.
     +-- ИСПРАВЛЕНИЕ: Либо используй результат, либо не вычисляй его.

[SKILL ISSUE :: W0301] in examples/calc.smpc:18:17
  18 | 7 -> @div(2) => $int_quotient;           // 3: целое деление отбрасывает дробь
     |                 ^~~~~~~~~~~~~
     |-- ОШИБКА: Записанное значение ни разу не прочитано.
     |-- ДЕТАЛИ: В регистр $int_quotient записано значение, но ни одна инструкция его не читает.
     |-- ДИАГНОЗ: Система типов существует именно для таких, как ты.
     +-- ИСПРАВЛЕНИЕ: Либо используй результат, либо не вычисляй его.

[SKILL ISSUE :: W0301] in examples/calc.smpc:20:15
  20 | 2 -> @sqrt => $root2;                    // корню нужна дробь, 2 становится 2.0
     |               ^~~~~~
     |-- ОШИБКА: Записанное значение ни разу не прочитано.
     |-- ДЕТАЛИ: В регистр $root2 записано значение, но ни одна инструкция его не читает.
     |-- ДИАГНОЗ: Компилятор знает тип каждого выражения. Ты, похоже, нет.
     +-- ИСПРАВЛЕНИЕ: Либо используй результат, либо не вычисляй его.

[SKILL ISSUE :: W0301] in examples/calc.smpc:21:16
  21 | -12 -> @abs => $modulus;                 // 12
     |                ^~~~~~~~
     |-- ОШИБКА: Записанное значение ни разу не прочитано.
     |-- ДЕТАЛИ: В регистр $modulus записано значение, но ни одна инструкция его не читает.
     |-- ДИАГНОЗ: Ранг и форма — часть типа. Отмахнуться от них не выйдет.
     +-- ИСПРАВЛЕНИЕ: Либо используй результат, либо не вычисляй его.

[SKILL ISSUE :: W0301] in examples/calc.smpc:24:47
  24 | $quotient -> @mul($quotient) -> @add(1.75) => $sq;    // 3.5^2 + 1.75 = 14
     |                                               ^~~
     |-- ОШИБКА: Записанное значение ни разу не прочитано.
     |-- ДЕТАЛИ: В регистр $sq записано значение, но ни одна инструкция его не читает.
     |-- ДИАГНОЗ: Вывод типов сработал. Не сработал ты.
     +-- ИСПРАВЛЕНИЕ: Либо используй результат, либо не вычисляй его.

[SKILL ISSUE :: W0301] in examples/calc.smpc:29:24
  29 | *&xs -> @reduce.max => $top;              // 42
     |                        ^~~~
     |-- ОШИБКА: Записанное значение ни разу не прочитано.
     |-- ДЕТАЛИ: В регистр $top записано значение, но ни одна инструкция его не читает.
     |-- ДИАГНОЗ: Ты требуешь операции, которая для этого типа не определена.
     +-- ИСПРАВЛЕНИЕ: Либо используй результат, либо не вычисляй его.

[SKILL ISSUE :: W0301] in examples/calc.smpc:40:45
  40 | *&v -> @mul(*&v) -> @reduce.add -> @sqrt => $norm;   // sqrt(2)
     |                                             ^~~~~
     |-- ОШИБКА: Записанное значение ни разу не прочитано.
     |-- ДЕТАЛИ: В регистр $norm записано значение, но ни одна инструкция его не читает.
     |-- ДИАГНОЗ: Пять типов. Ты умудрился перепутать два из них.
     +-- ИСПРАВЛЕНИЕ: Либо используй результат, либо не вычисляй его.
$product      <i32>  391
$difference   <i32>  63
$int_quotient <i32>  3
$quotient     <f64>  3.5
$root2        <f64>  1.4142135623731
$modulus      <i32>  12
$sq           <f64>  14
$mean         <f32>  18
$top          <f32>  42
$norm         <f32>  1.4142135
*&xs          <f32:6>  [4, 8, 15, 16, 23, 42]
*&M           <f32:2,3>  [1, 2, 3, 4, 5, 6]
*&v           <f32:3>  [1, 0, -1]
*&Mv          <f32:2,1>  [-2, -2]
*&bad_idea    <f32:3>  [1, inf, -3]
*&T           <i32:3,2>  [0, 10, 0, 20, 0, 30]
*&H           <f32:4>  [18, 18, 18, 18]
*&H2          <f32:4>  [63, 63, 63, 63]
