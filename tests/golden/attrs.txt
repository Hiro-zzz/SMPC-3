$ smpc3 attrs
--- код возврата: 0
Префикс — директивы:
  #arena          номер арены, 0..7
  #simd           ширина вектора: v128, v256, v512
  #unroll         коэффициент разворота цикла
  #repeat         развернуть инструкцию N раз, 1..4096
  #index          имя индекса повтора для #repeat

Префикс — режимы:
  ^raw            сырой указатель, вывод шага отключён
  ^readonly       запись в операнды запрещена

Суффикс — утверждения:
  !no-alias       операнды не делят память
  !aligned        база выровнена под текущую ширину вектора
  !contiguous     операнды плотные

Суффикс — модификаторы:
  ?strict         любое отступление от запрошенного — фатально
  ?fast           разрешены переассоциация и приближения

Суффикс — fp-режимы:
  ~flush-to-zero  FTZ+DAZ в MXCSR
  ~precise        без переассоциации

